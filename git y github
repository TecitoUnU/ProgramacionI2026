Miguel Angel Lool Ortiz
0907-25-13633

Respuestas: 
¿Que es git?
Git es una herramienta que guarda el historial de todos los cambios en un proyecto que nos permite volver a versiones anteriores para ver los cambios que se han realizado.

¿Cuáles son los comandos más importantes y para qué sirven?

git init — Empieza un repositorio de Git en tu carpeta.
git clone [url] — Copia un proyecto que ya está en internet.
git status — Es una consulta del estado actual del proyecto.
git add [archivo] — Agrega archivos a la lista para guardar y "trackear".
git commit -m "mensaje" — Guarda los cambios con un mensaje.
git pull — Baja los cambios nuevos de una copia en línea.
git push — Sube tus cambios a la copia en línea.
git branch — Muestra o crea ramas o línea de trabajo.
git checkout — Cambia de rama o versión del ptoyecto.

¿Cuál es la diferencia entre Git y GitHub?
GIT es el programa que guarda los proyectos de manera local para que podamos ver los cambios anteriores que hemos realizado, sin necesidad de internet al ser de manera local. GitHub es un sitio en Internet donde se pueden subir nuestros proyectos en local a la nube, compartirlo, colaborar con otros y ver el proyecto desde una web.

Enumere los comandos para subir cambios a un repositorio
	1) git add
	2) git commit -m
	3) git push

¿Qué es una branch?
Una branch es una versión alterna del proyecto que se puede utilizar para hacer cambios y ecperimentar sin afectar el proyecto principal. Al final estos cambios realizados en el branch pueden subirse con la versión principal para mantener los cambios ya realizados fuera del proyecto principal.
